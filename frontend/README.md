# SkillShare Frontend

React frontend scaffold for a MERN SkillShare-style learning platform.

## Scripts

```bash
npm install
npm run dev
npm run build
```

The app uses mock data now. Later, replace the data files in `src/data` with API calls to your Express backend.
